# FJSP-SDST 禁忌搜索：现有方案、已尝试方案与未来路线图

> 基于 `maser-0911` 工程当前源码（C++20，复现 Shen et al. 2018 的 FJSP-SDST 禁忌搜索）实读整理。
> 整理时间：2026-09-12（覆盖同日 12:51 的早期版本，内容大幅扩展）。
> 配套评测数据：`benchmark_report.csv`（300k）、`benchmark_report_1M.csv`（1M，非 SDST）、`sdst_report.csv` / `sdst_report_1M.csv`（SDST 可行性 + Cmax）。
> 说明：文中所有"早期方案 / 已回退 / 效果一般"结论，均来自源码中保留的对照注释与实测数据，非臆测。

---

## 0. 项目概况与当前战绩

- **问题**：柔性作业车间调度带顺序相关准备时间（FJSP-SDST），最小化最大完工时间 Cmax。
- **算法**：Shen et al. (2018) 的禁忌搜索（Tabu Search），析取图建模 + 关键块邻域（N5/N6）+ 下界引导 + 混合扰动 + ILS 式拉回。
- **规模**：157 个非 SDST 算例（Brandimarte 10 + Dauzère 18 + Hurink edata/rdata/vdata 各 40 + mt 3）+ 157 个 SDST 变体 = **314 个**。
- **当前最优战绩（1M 迭代 / top-K=8 / 单关键路径）**：
  - 非 SDST：**41/157 达到下界（已证最优，26.1%）**，gap_LB 均值 **1.74%**，0 个超越已知文献最优解。
  - SDST：**157/157 全部可行**（独立重解码交叉校验通过），Cmax 平均比非 SDST 基线高 **13.02%**。
- **关键约束**：每算例 60s 时间上限是兜底，27 个算例触发（TimedOut=1），它们是剩余 gap 的主要来源。

---

## 1. 现有技术方案（当前代码状态）

### 1.1 建模：析取图 + SDST 集成
- 用 `Graph`（`src/Graph.cpp`）维护 **析取图**：工序为节点，工序内（job）边为**强制边**，机器内（machine）边为**析取边**（由调度选择方向）。
- **SDST 集成关键点**：准备时间 `sdst_matrix[m][prev_job][next_job]` 出现在三处（必须同时一致，否则关键路径追踪与下界都会错）：
  1. 初始解构造时机器可用时间的起点（`Graph.cpp:655`）。
  2. 关键路径追踪时同机器后继的衔接判据（`TabuSearch.cpp:247, 324`）。
  3. `update_time()` 的正/反向最长路计算（`Schedule.cpp:47-49, 95-99`），`r_machine`/`q_machine` 均含 SDST。
- 完整性由 `is_legal_move()`（`Schedule.cpp:231`）用 `forward_path_length` 比较检测**成环**，移动非法直接拒绝。

### 1.2 初始解：GRASP + MWKR + top-K 机器随机
- `Graph::heuristic_init()`（`Graph.cpp:527`）：分派规则为 **MWKR（剩余工作量最大优先）**，工序在 MWKR 前 **K=3** 名里随机（20% 概率完全随机保多样性）；机器在完工时间前 **K=2** 名里随机。
- 多起点：**每个算例构造 `init_attempts`（默认 5）次**，取 makespan 最小者作搜索起点（`Solver.cpp:32-41`）。这把"纯随机初始解"的劣质起点（如 dpp01a 初始 5002 vs 最优 2505）大幅压低，成本极低。

### 1.3 邻域结构：N5/N6 四类移动 + 两点增强
`find_move()`（`TabuSearch.cpp:11`）对每个关键块内工序按 `get_move_case()` 分类（Case I/II/III/IV，**Case IV 与未关键工序不生成移动**），生成：
- **Type 1**：同机器向后插（BACK，Case I/III）→ `TabuSearch.cpp:63`
- **Type 2**：同机器向前插（FRONT，Case II/III）→ `TabuSearch.cpp:102`
- **Type 3**：换机器向后插（CHANGE_MACHINE_BACK）→ `TabuSearch.cpp:80`
- **Type 4**：换机器向前插（CHANGE_MACHINE_FRONT）→ `TabuSearch.cpp:119`

两点增强（均为已落地、有效的改进）：
- **【P0-1】块端点跳跃**（`TabuSearch.cpp:53-58, 72-78, 111-117`）：除紧邻的 v/w，允许一步跳到块首/块尾，块长 k 时动作数从 k-1 提升到 2(k-1)，对齐标准 N5。
- **【P0-2】保留单例块**（`TabuSearch.cpp:267-295`）：不再丢弃"独占一台机器"的长度为 1 的关键工序，能否移动交给 `get_move_case` 剪枝决定，使邻域从个位数扩大到完整关键路径（实测关键路径长 11–22）。

### 1.4 下界 LB1/LB2（含 SDST）+ 移动分类
- 同机器移动用 **LB1 = max(r_job + p + q_job, r'_u + p + q'_u)**（`same_machine_evaluate_and_push`，`TabuSearch.cpp:458`）。
- 换机器移动用 **LB2 = max(r_job + p_new + q_job, r'_u + p_new + q'_u)**（`change_machine_evaluate_and_push`，`TabuSearch.cpp:523`）。
- 两式中的 `r'_u`/`q'_u` 均正确叠加了新产生的 **SDST 准备时间**（`:443, 454, 508, 519`），这是 SDST 版本与基础 FJSP 版的本质差异。
- `get_move_case()`（`Schedule.cpp:283`）依据工序在关键路径上的位置（同时满足 r+p+q 的哪一种组合）判定 Case I–IV，决定可施加的移动类型。

### 1.5 精确评估：LB top-K=8 完整解码
- LB1/LB2 只是下界估计，用它排序选出的"最优"移动只有 15–22% 恰好是完整解码后的真实最优（但真实最优 100% 落在 LB 前 3 名内）。
- 做法：对 LB 最小的 **K=8** 个候选做完整 `make_move + update_time` 解码（`TabuSearch.cpp:155-191`），按真实 makespan 择优。代价是每轮多 8 次解码，换取显著更好的移动选择（这是 gap 从早期 9.75% 降到 2.38% 的核心改动之一）。
- 被禁忌的候选记 `INT_MAX`，不参与 top-K 排序（`:472, 537`）。

### 1.6 禁忌机制：动态长度 + 特赦 + 安全回退
- 禁忌表固定容量 `tabu_list(2000)`（`TabuSearch.h:40`），**这是一个潜在隐患**（见 §2.3）。
- 动态禁忌长度 `T = iteration + rand_int(L, L_max)`，其中 `L = 10 + job_num/machine_num`，`L_max = L*1.4 或 1.5`（`TabuSearch.h:41-42`）——随算例柔性自适应。
- **特赦准则**（`is_tabu`，`TabuSearch.cpp:388`）：若移动使 makespan 优于历史最优，立即解禁。
- **成环安全回退**（`search`，`TabuSearch.cpp:911-939`）：先备份无环状态，执行移动触发拓扑环异常则瞬间回退，并将该移动以**普通禁忌周期 L** 封禁（早期曾用 999999 永久封禁，会把"此刻成环、换个上下文就合法"的动作永久锁死，已改为 L）。

### 1.7 混合扰动 + ILS 式拉回
- `apply_perturbation()`（`TabuSearch.cpp:805`，**现行版**）：三类算子配比 **35% 换机器 / 45% 同机器重插 / 20% 同机器交换**；候选池 80% 取关键工序、20% 放宽全体；强度 `intensity` 随连续无效扰动次数放大（`1.0 + 0.15*perturb_since_improve`）；目标破坏约节点数 15%。换机器对单候选工序无效时自动退化为重插，保证柔性小的算例（edata 等）也能被扰动。
- **旧版**（仅换机器头/尾，`TabuSearch.cpp:643` 已注释）对柔性小算例几乎空转，已被上述混合版取代。
- **ILS 拉回**（`search`，`TabuSearch.cpp:894-896, 957-960`）：连续 `kMaxPerturbNoImprove=12` 次扰动仍不刷新历史最优 → 拉回 `best_schedule` 重新出发，避免越扰动越差。
- 无改进迭代上限 `max_non_improve = max(5000, node_num*30)`（`TabuSearch.cpp:863`），达到即触发一次扰动。

### 1.8 多重启与可复现性
- 每算例可在 `main` 层做 `restarts`（默认 1）次独立重启取最优（`main.cpp:163-172`）。
- **种子可复现**：`derive_seed(seed, idx, restart)`（`main.cpp:41`）为每 (算例, 重启) 派生子种子，`rng::reseed` 重置随机流，保证"逐例、逐重启"结果完全可复现，且增删算例不影响其他算例结果。

### 1.9 终止条件与 SDST 可行性校验
- **主终止条件为迭代次数**（`max_iterations_`，`TabuSearch.h:44`），时间上限仅兜底（`TabuSearch.h:32-33`）。
- **时间检查（2026-09-12 17:45 已改）**：原实现为**每 64 轮读一次时钟**（`TabuSearch.cpp` 旧逻辑，注释仍保留在 `:874` 附近），单轮慢时以「64×单轮耗时」为粒度严重过冲（实测 ~940–1010s）。现已改为**逐轮检查** `steady_clock::now()`：`now()` 开销仅数十纳秒，相对单轮搜索（微秒~毫秒级）可忽略，过冲从 64 轮压缩到 1 轮；触发则 `timed_out_=true`。注意：逐轮检查下，慢算例会真正在 ~60s 停下（迭代次数变少），因此需与 §3.B2 的「慢算例更大预算」配套，否则质量会略降。
- SDST 结果由 `tools/verify_schedule.py` **独立重解码**校验可行性并回算 Cmax，与求解器输出逐例比对（`make_sdst_report.py` 调用），1M 轮 157/157 完全一致。

### 1.10 零分配回滚与探针缓冲（2026-09-12 20:5x 已落地，★ 最大性能杠杆）
- **背景（根因）**：改动前每一代要做约 **100 次堆分配**——
  1. `search()` 里 `Schedule backup_schedule = current_schedule;`（回滚备份）；
  2. `find_move()` 的 top-K 精确评估里，对每个候选 `Schedule probe = current_schedule;`（最多 K=8 次）；
  3. 扰动算子 `try_apply_move()` 里 `const Graph backup = current_schedule.graph;`。
  每个 `Schedule` = `Graph` 的 11 个 `vector` + `time_info`，共约 12 次 `new/delete`。30 万代即约 **3000 万次堆分配**。
- **后果**：堆碎片化 → **单代耗时随运行时间暴涨（实测最高 76 倍）** → 病态算例触发 60s 上限被截断 → 迭代数不一致 → **结果不可复现**（见 §4.4）。
- **做法**：把上述三处都换成**复用成员缓冲区**（`TabuSearch.h` 新增 `backup_graph_` / `undo_graph_` / `probe_schedule_`），`vector` 的拷贝赋值会复用已有容量，**稳态下零分配**。
  - 只备份 `Graph` 而不备份 `time_info`/`makespan` 的依据：成环异常由 `update_time()` 中的 `topological_sort` 抛出（`Graph.cpp:38`），而它在写入任何时间信息之前执行（`Schedule.cpp:24` 先于 `makespan = 0`），故异常时二者仍是移动前原值。
  - 顺带删除 top-K 里**冗余的第二次 `update_time()`**（`Schedule::make_move` 内部已调用），top-K 代价直接减半。
  - 顺带修复 `Graph::node_num` 未初始化（`int node_num{};`）。
- **验证一：300k 档**（非 SDST 159 个，与同配置基线 `runs/timecheck` 对照）：

  | 指标 | 基线 | 消除深拷贝后 |
  |---|---|---|
  | 总时长 | 5848s (1.62h) | **921s (0.26h)** ← **6.35×** |
  | 平均 / 算例 | 36.8s | **5.8s** |
  | 最大单算例 | 932.4s | **20.5s** |
  | TimedOut | 6 | **0** |
  | 质量（Cmax） | — | 变好 4 / 变差 **0** / 持平 155 |

- **验证二：1M 档**（与旧 1M 基线 `runs/fulleval/*_1M` 对照）：

  | 指标 | 旧 1M | 新 1M |
  |---|---|---|
  | 非 SDST 总时长 | 16234s (4.51h) | **3357s (0.93h)** ← **4.84×** |
  | 非 SDST 最大单算例 | 1009.6s | **247.6s** |
  | 非 SDST TimedOut | 27 | **3** |
  | 非 SDST gap_LB / gap_UB | 1.74% / 1.61% | **1.70% / 1.57%** |
  | 非 SDST 达下界 | 41 | 41 |
  | 非 SDST 逐例 Cmax | — | 变好 10 / 变差 1 / 持平 148 |
  | SDST 总时长 | 16289s (4.52h) | **3515s (0.98h)** ← **4.63×** |
  | SDST TimedOut | 29 | **5** |
  | SDST Cmax 增量 | +13.02% | **+12.92%**（157/157 可行，交叉校验一致） |

  > 1M 档达下界仍为 41（未增加），但 gap 下降、10 个算例变好（多为原先被截断的 dauzere/rdata 难例），只有 dauzere-17a 因单代仍慢被截断而微差（2184→2211）。

- **正确性**：20k 代下新旧二进制同目录同种子逐例比对，**159/159 的初始解、最终 Cmax、迭代数完全一致** → 搜索轨迹零改变。
- **附带收益**：`TimedOut = 0` 意味着不再有算例被时间上限截断，**结果恢复完全可复现**（此前"一旦触发时间上限即不可复现"的问题被根治）。

---

## 2. 之前已尝试但效果一般 / 已被回退的方案

### 2.1 【实测负向，已回退】item3：每轮看全部关键路径（多路径）
- **做法**：实现 `update_all_critical_block()`（`TabuSearch.cpp:540`，从所有关键路径起点各自追踪、跨路径去重、保留单例块，并修复了原 BFS 截断 bug），让 `find_move` 在**所有关键块**上生成移动，而非单条随机路径。
- **假设**：更多关键块 → 更全面的邻域 → 更好解。
- **实测结果（6 难例 AB 测试）**：
  | 迭代 | 单随机路径 | 多路径（item3） | 多路径相对 |
  |---|---|---|---|
  | 300k | gap 8.06% | 9.54% | **+1.48pp（更差）** |
  | 1M | gap 6.84% | 7.57% | **+0.73pp（更差）** |
- **结论与归因**：多路径是**负向优化**，已回退 `find_move` 到 `update_critical_block()`。归因：单条随机路径天然提供了"随机游走式"探索多样性，而每轮固定覆盖全部关键块反而**稀释了 LB 信号的区分度**，并放大了禁忌冲突。该函数代码保留但未使用（可作为未来"自适应切换单/多路径"的素材）。
- **教训**：对 TS 而言，"每轮探索范围更大"不必然更好，关键块的**随机性采样**本身就是有效的多样化机制。

### 2.2 【改进前朴素基线，作为对照】早期实现被现有方案取代的若干做法
以下并非"失败的实验"，而是本工程**改进前的初始状态**，列于此以说明现有方案为何这样设计、以及它们的效果短板：
- **纯随机初始解**（`Graph::random_init`，`Graph.cpp:130`，仍保留但默认不用）：可调度工序均匀随机，初始解质量极差（dpp01a 初始 5002 vs 最优 2505），全靠 TS 兜底 → 被 GRASP+MWKR 取代。
- **仅换机器头/尾邻域**（旧 `apply_perturbation`，`TabuSearch.cpp:643` 注释）：对柔性小算例（edata 每工序仅 2 台候选、大量仅 1 台）几乎空转，数百次扰动困在同一吸引域 → 被混合扰动取代。
- **size>1 块过滤**（`update_critical_block` 旧逻辑）：关键路径上"独占机器"的工序被整体丢弃，邻域仅个位数 → 被 P0-2（保留单例块）取代。
- **仅相邻交换**：块长 k 时只能做 k-1 个相邻交换 → 被 P0-1（块端点跳跃）取代。
- **仅 LB 选优**：直接用 LB1/LB2 排序选移动，约 80% 概率选非真实最优 → 被 top-K=8 完整解码取代。
- **成环永久封禁**（旧禁忌长度 999999）：把大量"换个上下文就合法"的移动永久锁死，邻域越搜越小 → 改为普通周期 L。
- **无扰动**：易早熟收敛 → 被混合扰动 + ILS 拉回取代。

### 2.3 【保留但存隐患】当前已知的设计债
- **`TabuList` 容量硬编码 2000**（`TabuSearch.h:40`）：对超大算例或长迭代可能溢出/复用，属潜在隐患，暂未触发问题。
- ~~**60s 时间上限被粗粒度检查架空**（每 64 轮才查一次）~~ → **已于本次修复**（见 §1.10 / §3-B1）。修复后仍有少量过冲，真正的根因是每代全量深拷贝导致的堆碎片化，也已一并解决（§1.10）。
- **Case IV 不生成任何移动**（`TabuSearch.cpp:34`）：标准 N5/N6 中 Case IV 某些情形仍可施加受限移动，当前直接跳过，可能漏掉部分有效邻域。

---

## 3. 未来打算尝试的技术方案与改进方案

> 按**优先级**排序。★ 表示性价比/收益最高。

### ★ A. 增量评估（Delta / Incremental Evaluation）【最高优先级】
- **动机**：当前每个候选移动都走 `update_time()` 做全图正/反向最长路重算（`Schedule.cpp:16`），这是性能主瓶颈——1M 迭代对多数算例只需 15–35s，但邻域大时单轮极慢，直接导致 27 个难例被时间/迭代上限截断。
- **做法**：移动只改变局部拓扑，只需更新受影响的工序及其下游反向最长路（标准做法是沿"受影响集"做 O(路径长) 增量更新），配合 top-K 的完整解码也只需对 probe 做增量。
- **预期收益**：单轮速度可提升 1–2 个数量级，难例能在同样预算下跑更多迭代，直接压低剩余 gap。
- **代价/风险**：增量更新边界易错（尤其 SDST 下准备时间沿路径传播），需以 `update_time()` 为 oracle 做对拍校验。

### B. 时间 / 禁忌 / 终止条件修正
- **B1 细粒度时间检查** ✅ **已落地（2026-09-12 17:45）**：`TabuSearch.cpp` 的"每 64 轮"已改为**逐轮检查**。实测总时长 3.22h→1.62h、达下界持平 33。但**未根除**残余过冲（仍有 4 个算例 ~930s），根因是每代全量深拷贝造成的堆碎片化 —— 该根因已由 §1.10 解决（930s → 20.5s）。
- **B2 提高每算例时间/迭代上限**：对那 27 个 TimedOut 难例单独给更大预算（如 300s 或 3M 迭代），验证能否进一步逼近下界。
- **B3 反应式禁忌长度（Reactive Tabu）**：根据近期是否陷入重复解动态拉长/缩短 T，替代当前固定 `rand(L, L_max)`。
- **B4 修复 `TabuList(2000)` 硬编码**：改为随算例规模设定，或改用哈希/开放式结构。
- **预期收益**：B1+B2 直接针对当前剩余 gap 的主因；B3 提升跳出吸引域能力。

### C. 规范化的 ILS（Iterated Local Search）
- **动机**：当前扰动强度与拉回逻辑较"手调"（`TabuSearch.cpp:805, 894`）。
- **做法**：引入**接受准则**（如 Late Acceptance Hill Climbing 的滑动窗口阈值，或历史最优 + 当前最优双接受），并把扰动强度做成**基于无改进代数的自适应调度**（类似自适应大邻域搜索的冷却/加热）。
- **预期收益**：更稳的全局探索，对 Dauzère 等难族尤其有益（当前 0/18 达下界）。

### D. 增强邻域与 Path Relinking
- **D1 块外插入 / N7 类移动**：当前邻域聚焦关键块内（且 Case IV 跳过），可加入"把非关键工序插入关键机器"等块外移动，扩展可行改进空间。
- **D2 Path Relinking**：在若干精英解（重启得到的局部最优）之间做"路径连接"，沿连接轨迹搜索高质量中间解——对多峰 landscape 很有效，且能与现有重启框架自然结合。
- **预期收益**：突破当前邻域"只能动关键工序"的天花板，可能触达更多未达下界算例。

### E. GA-TS / ALNS 混合
- **动机**：纯 TS 的单轨迹搜索对超难算例易陷深吸引域。
- **做法**：以**种群（GA）或自适应大邻域搜索（ALNS）** 维护多样性，个体/解用当前 TS 做局部精炼；ALNS 的 destroy/repair 算子可直接复用现有"换机器/重插/交换"扰动。
- **代价/风险**：实现复杂度高，需重新设计可复现性与实验对照；建议作为"第二阶段"投入。

### F. 并行与岛模型
- **动机**：当前完全单线程、单轨迹；314 个算例虽可并行跑（已这样做），但**单算例内**未并行。
- **做法**：① 单算例内用多线程并行评估 top-K 候选（配合 A 的增量评估收益最大）；② 多 Island 协作，各岛独立 TS + 周期性迁移精英解。
- **预期收益**：在相同时钟内成倍扩展搜索；对 1M+ 迭代的难例尤其关键。

### G. 更强下界与剪枝
- **动机**：LB1/LB2 仅是 O(1) 估计，top-K 选出的候选里仍有 ~20% 非真实最优（§1.5）。
- **做法**：① 引入 LB3/LB4（如考虑机器负载均衡、工件剩余路径的更紧下界）；② 在 `find_move` 阶段用更紧下界**提前剪掉不可行候选**，减少完整解码次数。
- **预期收益**：在同等 K 下提升 top-K 命中率，或同等质量下降低解码开销。

### H. 多目标扩展
- **动机**：SDST 场景常关注总准备时间、拖期等，而非仅 Cmax。
- **做法**：把目标扩展为 (Cmax, 总 setup time) 帕累托前沿，扰动/接受准则改为多目标支配判断。
- **代价/风险**：需重新设计下界与禁忌语义；属于"延伸方向"而非逼近现有基准的捷径。

### I. 学习式组件
- **动机**：移动选择、禁忌长度、扰动强度目前均规则化。
- **做法**：用轻量 RL / 元学习根据"当前 landscape 特征（关键块数、LB 方差、无改进代数）"动态选移动类型与 T；或用神经网络近似增量评估（替代 A 的手写公式）。
- **代价/风险**：训练成本与可复现性挑战大，优先级最低。

### J. 真实数据基准
- **动机**：当前 SDST 算例是**合成**的（由非 SDST 基准加合成准备时间生成，`instance_SDST/`），且 SDST 无公开文献下界，无法评 gap。
- **做法**：接入真实 FJSP-SDST 工业/学术基准（若有），建立可评 gap 的 SDST 评测体系。
- **预期收益**：让 SDST 结果也能与文献横向对比，而不仅停留在"可行性 + 相对非 SDST 的 Cmax 增量"。

---

## 3.5 阶段一落地清单（2026-09-13 完成）

**目标**：在严格确定性、搜索轨迹逐例零改变的前提下，消除性能瓶颈，让"同样墙钟跑更多迭代"。

| 编号 | 改动 | 类型 | 是否保留 | 效果 |
|---|---|---|---|---|
| P1 | 探针改 forward-only 评估（`eval_makespan_only`，不重算反向尾量） | 性能 | ✅ 保留 | 探针约 1.27× |
| P2+P4 | `update_time` 融合 Kahn 拓扑+正向松弛，环检测由抛异常改返回 bool，支持提前中止 | 性能/正确性 | ✅ 保留 | 提速 + 去异常 |
| P5 | SDST 准备时间表摊平为 1D（`setup_flat`） | 性能 | ✅ 保留 | 消除三级间接寻址 |
| P6 | top-K 改用 `partial_sort` + 复用成员缓冲，去掉每代 `new vector` | 性能 | ✅ 保留 | 消除每代分配 |
| P7 | `TabuList` 容量由硬编码 2000 改为 `op_num+3`（修复哨兵 id=node_num 越界 bug） | 性能/正确性 | ✅ 保留 | 省内存 + 修禁忌丢键 |
| P9 | 删除无读取点的 `on_machine_pos_vec`（10 处写入 + 沿链 O(n) 扫描） | 性能 | ✅ 保留 | 省 make_move 全扫描 |
| P8 | `find_move` 内部 deadline（每若干候选检查时间，超时即返回） | 正确性 | ✅ 保留 | 根治 15× 时间过冲（设定 5s→实测 5.0s） |
| P11 | 动态 top-K（LB 打平时扩 K 到 32） | 策略 | ❌ **否决** | 代表集 1M：变好 8 / 变差 8，整体打平且更慢 |
| P13 | 重启多样性（restarts 2/4） | 策略 | ❌ **否决** | P0 实验：restarts 越多越差（1M 平均 gap 3.07%→3.35%→3.88%） |

**阶段一合计**：单线程 ~1.70× 提速（20k 档逐字段保轨迹校验通过），无质量变化。

**关键实验结论（P0 预算分配）**：等预算下"一次长跑"严格优于"多次短跑" → 全部预算应投在单条长轨迹上，重启/多样性路线取消。

---

## 4. 实验证据与瓶颈诊断

### 4.1 评测口径
- 非 SDST：对照 FJSPLib 文献下界 LB/上界 UB，gap = (Cmax − LB)/LB；"达下界" = Cmax == LB == UB（已证最优）。
- SDST：无基准，评**可行性**（独立重解码校验）+ **Cmax 相对非 SDST 基线的增量**。
- 配置标记：`300k` / `1M` 迭代、`top-K=8`、单关键路径（Shen2018 tabu）。

### 4.2 非 SDST：300k vs 1M 对比
| 指标 | 300k（旧） | 1M（新） | 变化 |
|---|---|---|---|
| 达下界（已证最优） | 33（21.0%） | **41（26.1%）** | +8 |
| gap_LB 均值 | 2.38% | **1.74%** | −0.64pp |
| gap_UB 均值 | 2.38% | **1.61%** | −0.77pp |
| 优于已知最优解 | 0 | 0 | 无新纪录 |
| 触发 60s 上限的算例 | 12 | 27 | +15 |

分族达下界（1M）：Brandimarte 6/10、Dauzère 0/18、Hurink-edata 18/43、Hurink-rdata 5/43、Hurink-vdata 12/43。
**Dauzère 18 个全是 LB<UB（最优值未知），理论无法证最优，符合预期**；其余未达下界者集中在被时间/迭代上限截断的难例。

### 4.3 SDST：1M 结果
- 157/157 全部可行，Solver_Cmax 与独立重解码 Verified_Cmax **完全一致**（交叉校验通过）。
- Cmax 平均比 1M 非 SDST 基线高 **13.02%**（准备时间导致的结构性抬升，合理）。

### 4.4 当前瓶颈诊断
- 剩余 gap **主要来自被 60s 时间上限 / 1M 迭代上限截断的难例**（非 SDST 27 个、SDST 29 个 TimedOut），而非算法"卡死"（159/159 算例均跑满预算、无提前收敛）。
- 增大预算（1M vs 300k）确实有效（达下界 33→41，gap 2.51%→1.74%，最大 gap 20.36%→11.44%）。
- **（2026-09-12 20:5x 更新）瓶颈已被重新定位并解决**：原先归因于"预算不足"，实测真正的根因是**每代全量深拷贝 → 堆碎片化 → 单代耗时暴涨 76 倍 → 触发时间上限被截断**。落地 §1.10 后 300k 全量墙钟 1.62h→0.26h（**6.35×**）、最大单算例 932s→20.5s、**TimedOut 6→0**，且质量只升不降（变好 4 / 变差 0）。因此原先"用质量换墙钟"的担忧**已不成立**：现在不仅更快，还因不再截断而更准确、且完全可复现。
- **可复现性结论**：**不触发时间上限 → 结果完全可复现；一旦触发 → 依赖机器速度与堆状态，不可复现。** §1.10 使 TimedOut 归零，等价于让全量评测恢复了可复现性。
- **下一步最高杠杆动作**：**A（增量评估）** —— 现在堆分配已归零，剩余性能瓶颈纯粹是 `update_time()` 的**全图正/反向最长路重算**（每代 K=8 次）。把它改成沿"受影响集"的增量更新，是唯一还能带来数量级提升的改动。之后再上 C/D/E 等结构性增强。

### 4.5 阶段一落地 + 10M 迭代结果（2026-09-13）

**性能侧（阶段一 P1/P2/P4/P5/P6/P7/P9/P8 全部落地）**：
- 单线程 ~1.70× 提速（逐字段保轨迹校验通过，质量零改变）；
- P8 根治 `find_move` 时间过冲（设定 5s → 实测 5.0s，旧为 15× 过冲）；
- 配合 9 路分片并行，**10M 迭代非 SDST 全量在 ~1.9h 跑完**（旧 1M 串行约 0.93h）—— 即以约 2× 墙钟换 10× 迭代。

**10M 迭代 vs 旧 1M（非 SDST 157 例）**：

| 指标 | 1M | 10M | 变化 |
|---|---|---|---|
| 达下界（已证最优） | 41（26.1%） | **58（37.0%）** | **+17** |
| 变好 / 持平 / 变差 | — | 100 / 56 / 1 | 仅 1 例回退（la36_rdata，因旧值本身是被 60s 截断的产物） |
| 平均 gap_LB | 1.701% | **1.062%** | −0.64pp |
| 超 LB 合计（单位） | 3492 | **2247** | −1245 |
| 破纪录（≤文献 UB） | 0 | 0 | 20 例 LB<UB 仍未被打到（见下） |

**分族达下界（10M）**：Brandimarte 7/10、Dauzère 0/18、Hurink-edata 22/43、Hurink-rdata 10/43、Hurink-vdata 19/43。vdata/rdata 提升最显著（+7/+5）。

**决定性结论：剩余 gap 的瓶颈是纯粹的"迭代预算不足"，而非算法卡死**。
- 14 个"只差 1 单位"的算例子集，随迭代单调收敛：1M→4/14 达下界、3M→7/14、10M→11/14（超 LB 合计 15→3，gap 0.16%→0.03%），**尚无边际递减**；
- Dauzère 18/18 仍 0 达下界（LB<UB，且平均 Cmax 偏高），属最难家族，单靠加迭代难以突破，需阶段二的结构性增强（C/D）。

**下一步（已获用户确认：目标子集加码）**：对 59 个"缺口≤15"的算例加码到 30M（接力脚本 `tools/chain_30m.sh` 自动在 SDST 10M 后启动）。其中 32 例缺口≤3（20 例只差 1），是达下界数的全部潜在增量来源。

---

## 5. 一句话总结

现有方案是一套**工程扎实、可复现的 Shen2018 TS 复现**：析取图 + SDST 感知下界 + top-K 精确解码 + 混合扰动 + ILS 拉回，1M 下非 SDST 达下界 41/157、gap 1.74%，SDST 全可行。已回退的"多关键路径"证明**盲目扩大每轮探索范围会稀释 LB 信号、反而变差**。

性能侧已完成阶段一全部微优化（单线程 1.70×、P8 根治时间过冲，逐字段保轨迹）+ 9 路分片并行，使**10M 迭代全量在 ~1.9h 跑完**，达下界从 41 推到 **58（+17）**、gap 1.70%→1.06%。决定性发现：**剩余 gap 几乎全部源于迭代预算不足**——"只差 1 单位"的算例随迭代单调收敛且无边际递减，下一步对缺口≤15 的 59 例加码 30M 即可进一步逼近（预期达下界 58→75 左右）。

被否决的方向：**动态 top-K（P11）**（代表集打平）、**重启多样性（P13）**（等预算下长跑严格更优）。**增量评估（A）** 经实测发现探针阶段根本不读反向量，已以 P1（forward-only）形式落地并见效，无需更复杂的增量更新。
